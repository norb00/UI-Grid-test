#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../hammer/bin/hammer" "$@"
  ret=$?
else 
  node  "$basedir/../hammer/bin/hammer" "$@"
  ret=$?
fi
exit $ret
